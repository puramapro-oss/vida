# VIDA (vida_sante) — ERRORS.md
# Format: |DATE|BUG|CAUSE|FIX|

| DATE | BUG | CAUSE | FIX |
|------|-----|-------|-----|
| 2026-08-15 | `REWARD_POOL_PERCENTAGE = 10` au lieu de `50` — pool de redistribution users sous-financé de 40 points | Constante mal réglée dès l'origine dans `src/lib/constants.ts` (`asso` correct à 10%, `sasu` non tracké en pool séparé — comportement existant conservé) | `10` → `50`. tsc 0, déployé, vérifié vivant (200) |
| 2026-08-15 | **CRITIQUE — `profiles.wallet_balance`/`pending_earnings` sont du code mort** | Colonnes lues côté dashboard (`profile?.wallet_balance ?? 0`) mais JAMAIS écrites nulle part dans le repo — 0 site d'écriture trouvé. Le vrai argent gagné (primes `prime_payouts`, commissions `referral_earnings`) est correctement tracé dans ses propres tables, mais jamais agrégé vers ce champ affiché à l'utilisateur. Trouvé en construisant la réconciliation wallet↔Stripe (cf task_plan.md) — pas corrigé (nécessite une décision de design : agréger à l'écriture, ou calculer à la lecture depuis les 2 tables sources) | **Non corrigé — hors périmètre de cette découverte, à traiter dans une passe dédiée** |
| 2026-08-15 | **CRITIQUE — table `wallets` absente en base live** | `handleInvoicePaid` (webhook) fait `db.from('wallets').select(...)`/`.update(...)` pour créditer la tranche 1 de prime (25€) immédiatement au checkout — la table n'existe pas en live, la requête échoue silencieusement (client Supabase JS ne throw jamais sans vérifier `.error`), le crédit n'a jamais lieu même si `prime_payouts.paid=true` est bien marqué. Même famille de gap que `satya` (`wallet_transactions`/`referrals` absentes, cf task_plan.md) | **Non corrigé — hors périmètre, nécessite de créer la table ou de rediriger le crédit vers un mécanisme existant** |
| 2026-08-15 | **CRITIQUE — tranches de prime J+30/J+60 jamais traitées** | Seule la tranche 1 (J+0) est créditée au checkout (et encore, cf ci-dessus). Aucun cron dans le repo ne traite les tranches 2/3 de `prime_payouts` (`scheduled_for` dépassé, `paid=false`) — elles resteront non payées indéfiniment. La réconciliation ajoutée ce jour (`/api/cron/reconcile`, check `overdue_unpaid_tranches`) détectera ce retard dès qu'un abonnement réel dépasse 30 jours | **Non corrigé — nécessite un cron dédié de traitement des tranches, à construire dans une passe séparée** |
